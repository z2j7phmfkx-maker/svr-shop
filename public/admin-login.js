'use strict';
const form=document.getElementById('loginForm');const error=document.getElementById('loginError');
form.addEventListener('submit',async event=>{event.preventDefault();error.textContent='';const button=form.querySelector('button');button.disabled=true;try{const response=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:document.getElementById('username').value,password:document.getElementById('password').value})});const result=await response.json();if(!response.ok)throw new Error(result.error||'Connexion refusée');const requested=new URLSearchParams(location.search).get('next');location.replace(requested&&requested.startsWith('/admin')&&!requested.startsWith('//')?requested:'/admin')}catch(value){error.textContent=value.message}finally{button.disabled=false}});
